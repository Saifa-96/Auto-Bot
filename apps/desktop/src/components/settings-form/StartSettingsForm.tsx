import { FC } from "react";
import { Node } from "reactflow";
import { StartSettings } from "../../core";

export const StartSettingsForm: FC<Node<StartSettings>> = () => {
  return <div>start settings form</div>;
};
